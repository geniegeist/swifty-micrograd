import XCTest
@testable import swifty_micrograd

final class swifty_microgradTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swifty_micrograd().text, "Hello, World!")
    }
}
